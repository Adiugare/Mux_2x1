# Mux_2x1
A 2x1 multiplexer (MUX) is a simple digital circuit that selects one of two input signals (a or b) and forwards it to a single output (y) based on the value of a control signal, called the select line (sel).
